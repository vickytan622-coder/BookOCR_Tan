# BookOCR_Tan 版本更新需求文档

本文档用于集中记录产品迭代需求。在进行下一次代码更新前，请先阅读本文档，确保改动与已规划方向一致。

## 当前状态

- 项目已开源至 GitHub： https://github.com/vickytan622-coder/BookOCR_Tan
- 当前处于真实用户操作测试阶段
- 采用“小步快跑、集中迭代”策略：先收集反馈，再统一修改

---

## 已确认需求（按优先级排序）

### P1：真正的一键工作流（方案 B）

**现状问题**

当前用户需要手动依次点击：
1. 开始识别
2. 生成原始 Markdown
3. 运行质量检查
4. （可选）开始 LLM 校对

对普通用户来说步骤过多，且“生成 Markdown”和“质量检查”本质上是 OCR 完成后应自动执行的本地操作。

**目标方案**

采用“默认自动 + 保留高级手动入口”的方案 B：

- 在 Web UI 顶部或创建任务区域增加一个主开关：
  - `☑ 创建任务后自动完成：OCR → Markdown → 质量检查`
- 当该开关勾选时：
  - OCR 100% 完成后自动调用 `assemble`
  - 自动生成 `*.raw.md`
  - 自动运行 `audit`
  - 在界面上直接显示中文结果摘要，无需用户滚动查看 JSON
- 当该开关未勾选时：
  - 保留现有的“生成原始 Markdown”和“运行质量检查”按钮
  - 或将其折叠到“高级操作 / 手动控制”区域
- 已有的 LLM “OCR 完成后自动开始校对”开关保持独立，避免意外消耗 API 额度

**待细化问题**

- 开关默认状态是“开”还是“关”？
  - 建议最终默认“开”，但测试阶段可暂默认“关”以便观察
- 自动执行失败时如何提示？
  - 应停在失败步骤，高亮显示错误，不阻塞后续可手动操作
- 是否需要记住用户上次选择？
  - 建议保存到 `~/Library/Application Support/BookOCR/settings.json`

**涉及文件**

- `src/bookocr/web.py`（前端开关、自动调用逻辑）
- `src/bookocr/config.py`（保存用户偏好）
- `README.md`（更新使用说明）

---

### P2：新增独立“设置”页面，集中管理全局配置

**现状问题**

当前 LLM 配置（Base URL、模型名、API Key、自动校对开关）直接放在“LLM 保守校对”区域，与一次性操作混在一起。随着“一键工作流”等全局偏好的增加，主页会越来越混乱。

普通用户：
- 不容易找到哪里修改默认行为；
- 不清楚哪些配置是“一次设置、长期复用”，哪些是“每次任务不同”。

**目标方案**

新增一个独立的“设置”页面/面板，把全局可复用配置从主流程中抽离。

**建议放入设置页的内容（全局持久化）**

| 设置项 | 当前位置 | 保存方式 |
|---|---|---|
| LLM Base URL | LLM 保守校对区 | `settings.json` |
| LLM 模型名 | LLM 保守校对区 | `settings.json` |
| LLM API Key | LLM 保守校对区 | macOS 钥匙串 |
| OCR 完成后自动开始校对 | LLM 保守校对区 | `settings.json` |
| 一键工作流开关（P1） | 创建任务区（计划） | `settings.json` |
| 默认使用 Apple 芯片加速 | 创建任务区 | `settings.json` |
| 默认输出目录 | 创建任务区 | `settings.json` |
| 每批领取页数（batch size） | 代码固定 50 | `settings.json` |
| 本机加速服务地址 | 高级设置折叠区 | `settings.json` |

**建议保留在主流程的内容（每次任务不同）**

| 配置项 | 说明 |
|---|---|
| PDF 路径 | 每次选择不同书籍 |
| 本次 OCR 页数 | 首次试跑或限定页数 |
| 本次校对前几页 | 测试校对效果 |
| 输出目录 | 可继承设置页默认值，但允许每次覆盖 |
| 是否使用 Apple 加速 | 可继承设置页默认值，但允许每次覆盖 |

**设置页面结构建议**

```
设置
├── LLM 模型
│   ├── Base URL
│   ├── 模型名
│   └── API Key
├── 工作流
│   ├── ☑ OCR 完成后自动生成 Markdown 并运行质量检查
│   ├── ☑ OCR 全部完成后自动开始校对
│   └── ☑ 创建任务时默认使用 Apple 芯片加速
├── 路径与输出
│   ├── 默认输出目录
│   └── 默认每批领取页数
├── 高级
│   ├── 本机加速服务地址
│   └── 查看日志 / 重置所有设置
└── 保存设置
```

**待细化问题**

- 设置页面是单独一页还是侧边栏/弹窗面板？
- 是否需要“恢复默认设置”按钮？
- 设置保存后是否立即生效，还是需要重启服务？
- 非法设置（如无法访问的 Base URL）是否要在保存时校验？

**涉及文件**

- `src/bookocr/web.py`（新增设置页 UI、读取/保存设置接口）
- `src/bookocr/config.py`（扩展 `SavedLlmSettings` 或新增全局设置模型）
- `README.md`（更新使用说明，说明设置页面）

---

### P3：MLX / OCR 后台运行，避免前台弹出终端窗口

**现状问题**

用户点击“启动 Apple 加速”或在 OCR 过程中，系统会弹出一个可见的 Terminal 窗口（`启动 MLX 加速服务.command`）运行 MLX VLM 服务。

普通用户：
- 不知道这个黑窗口是干什么的；
- 容易误以为是无关程序而关闭；
- 一旦关闭，MLX 加速会中断，但用户可能不知道原因。

**目标方案**

让 MLX 加速服务和 OCR 工作线程在后台运行，前台 Web UI 只保留进度条和状态提示。

具体方向：

- 点击“启动 Apple 加速”后，不再弹出可见 Terminal 窗口；
- MLX 服务以守护进程/后台子进程方式启动；
- 服务日志重定向到项目内日志文件（如 `output/mlx_server.log`），而不是显示在 Terminal；
- Web UI 继续通过 `/api/mlx/status` 轮询服务是否就绪；
- 如果服务异常退出，Web UI 显示明确提示（如“Apple 加速已停止，正在回退 CPU”）；
- 保留一个“查看 MLX 日志”的高级入口，供排障使用。

**待细化问题**

- 是否把 MLX 服务直接作为 BookOCR Web 服务的子进程管理？
  - 优点：生命周期一致，启动/停止都由 Web 服务控制；
  - 缺点：服务崩溃可能影响主进程稳定性。
- 还是继续用独立进程，但通过 `nohup`/`launchd`/`open -j` 隐藏窗口？
- 日志保留多久？是否需要自动轮转？

**涉及文件**

- `src/bookocr/web.py`（启动/停止 MLX 的逻辑）
- `启动 MLX 加速服务.command`（可选：改为后台启动脚本或保留高级排障入口）
- `README.md`（更新使用说明，去掉“保持终端窗口打开”的提示）

---

### 下一版重点：Windows 系统支持

**现状问题**

当前 BookOCR_Tan 仅支持 Apple Silicon Mac，核心依赖和交互方式都绑定在 macOS 上：

- 安装和启动使用 `.command` 脚本（macOS 专属）；
- 文件选择器调用 `osascript`（AppleScript）；
- API Key 保存使用 macOS 钥匙串（`security` 命令）；
- MLX 加速依赖 Apple Silicon 的 Metal GPU；
- 安装脚本检查 `uname -m` 是否为 `arm64`；
- 部分命令如 `lsof`、`open`、`caffeinate` 在 Windows 上不存在或行为不同。

**目标方案**

让 BookOCR_Tan 可以在 Windows 上安装和运行，优先保证 CPU 路径可用，加速可选。

具体方向：

- 提供 Windows 安装脚本（`.bat` 或 PowerShell）；
- 提供 Windows 启动脚本，双击即可启动 Web 服务并打开浏览器；
- 用跨平台方式替换 `osascript` 文件选择器：
  - 方案 A：Web 直接 `<input type="file">` 选择文件；
  - 方案 B：Python 的 `tkinter.filedialog`；
  - 方案 C：使用跨平台库如 `pydialog`/`zenity`。
- API Key 在 Windows 上改用加密本地文件或 Windows Credential Manager；
- MLX 加速在 Windows 上禁用或替换为 ONNX/DirectML 等替代方案；
- CPU 兜底路径（PaddleOCR CPU）必须能在 Windows 正常跑通；
- 路径处理统一使用 `pathlib`，避免硬编码 `/`；
- 检测系统平台，给不同平台展示对应提示。

**待细化问题**

- 是否优先支持 Windows 11 + WSL2，还是原生 Windows？
- Windows 上 Python 3.11/3.12 的安装方式（Microsoft Store / python.org / conda）？
- PaddleOCR 在 Windows 上的模型缓存路径和依赖是否稳定？
- 是否保留一个 macOS 专用分支，还是同一套代码多平台适配？

**涉及文件**

- 新增：`安装 BookOCR.bat` / `启动 BookOCR.bat`
- `src/bookocr/config.py`（跨平台 Key 存储）
- `src/bookocr/web.py`（跨平台文件选择、系统检测）
- `src/bookocr/ocr.py`（平台相关路径和加速后端）
- `README.md`（Windows 安装说明）

---

### P2：Web UI 界面美化

**现状问题**

当前 Web UI 是功能优先的简单排版：所有控件纵向堆叠、样式朴素、信息密度高。对普通用户来说：

- 第一眼不知道从哪里开始；
- 进度和状态不够醒目；
- 按钮层级不清晰；
- 没有响应式适配，窗口大小变化时体验不佳。

**目标方案**

在不改变核心功能的前提下，对 Web UI 进行视觉和交互优化：

- 采用更清晰的信息分区：顶部状态栏、左侧/顶部导航、主工作区；
- 创建任务、导出检查、LLM 校对使用卡片/标签页分隔；
- 进度条和状态文字更醒目，成功/失败/运行中用颜色区分；
- 按钮主次分明，核心操作突出；
- 增加空状态提示（例如未创建任务时的引导）；
- 响应式布局，适配不同窗口尺寸；
- 保持中文界面，使用非技术语言。

**设计原则**

- 工具感、专业感，不过度花哨；
- 浅色模式为主，后续可考虑深色模式；
- 优先保证功能可用，再逐步美化。

**涉及文件**

- `src/bookocr/web.py`（内嵌 HTML/CSS/JS）
- 可选：把 UI 拆分到独立静态文件（`static/` / `templates/`）

---

## 待讨论/观察中的需求

以下需求已在用户反馈或测试中发现，但尚未最终确认方案，需继续观察：

1. **OCR 进度显示优化**
   - 正常阶段进度条是否应包含 `running` 页？
   - 重试阶段“剩余 running 页”文案是否应改为“正在重试 running 页”？
2. **表格输出格式**
   - PaddleOCR-VL 输出的 HTML 表格是否应自动转换为 Markdown 表格？
3. **自动更新机制**
   - 是否需要内置版本检查/更新提示？
   - 用户当前需手动 `git pull` 或关注 GitHub Release

---

## 更新本文档的约定

- 每次收到新的产品反馈或迭代需求，先追加到本文档；
- 不要直接零散改代码；
- 当积累到一批可执行的改动时，统一进入一次迭代；
- 迭代开始前，所有参与者应先完整阅读本文档最新版本。

---

*最后更新：2026-07-15（包含 Windows 支持与 UI 美化需求）*
